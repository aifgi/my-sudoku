# ui-redesign

**Goal**: Modify UI to match the screenshot in the root folder (Screenshot 2026-05-27 at 22.38.15.png)

## Interview Format
- Version: 1.0

## Interview Responses

### Goal Interview
- Scope: Action buttons row (Undo, Erase, Notes[OFF/skip], Hints[count badge]); 3×3 number pad + New Game button; cell highlighting (row/col/box); timer + mistakes counter
- Notes mode: show button as OFF, skip implementation
- Hints: already implemented, just needs button with count badge
- All Time score: not in scope
- Fidelity: same layout, approximate blue color scheme
- Reference: `/workspace/Screenshot 2026-05-27 at 22.38.15.png`

### Requirements Interview (from requirements.md)
- Pencil/notes mode: REMOVE the pencil button entirely — no pencil feature at all
- Mistake limit: max 3 mistakes → show game-over dialog; player can start new game from dialog
- Hint display: fixed-height slot above numpad (always reserved); when hint shows, also select the hinted cell on the board
- Layout: full Row redesign — board left (~60%), controls right (~40%); fixes board/numpad vertical alignment and New Game button position
- Chosen approach: A — Full redesign (Row layout, 3×3 numpad, circular action buttons, 3-mistake game-over, hint slot with cell selection, blue color scheme, row/col/box highlighting)

### Tasks Interview (from tasks.md)
- Granularity: coarse (10-20 larger tasks)
- E2E verification: YES — final VE task: ./gradlew :app:run + visual confirm
- Chosen approach: Phased delivery — state changes first, then UI layer, then build verification

### Design Interview (from design.md)
- GameState model: isGameOver: Boolean flag (not sealed class) — simpler, minimal diff
- Chosen approach: Extend existing architecture — all changes in-place across GameState, GameViewModel, GameScreen, SudokuBoard, NumberPad; new GameOverDialog and color constants file

## Intent Classification
- Type: MID_SIZED
- Confidence: medium (UI modification with multiple components)
- Min questions: 3
- Max questions: 7
- Keywords matched: modify, UI

## Learnings (from design phase)
- `HintResult.Found` field name for target cell must be verified before implementing FR-7 — engine source not yet read; fall back to no selection change if field absent
- Fixed hint slot chosen at `56.dp` — fits one text line with padding; increase to `72.dp` if HintBanner can wrap
- Timer cancellation on game-over implemented inline in `dispatch()` post-update check rather than a new intent, consistent with how `GameCompleted` works
- Pencil/notes `BadgedActionButton` in current `GameScreen` is already UI-only (always disabled/OFF) — removal is safe, no reducer impact
- `NumberPad` already uses nested `Column + Row` with `weight(1f)` + `aspectRatio(1f)` on `Surface` — correct pattern, no structural change needed
- `SudokuBoard` canvas layers are already in the correct order per research.md; no layer reordering required
- `AppColors` placed in `sudoku.app.ui` package (not `components`) — shared by `GameScreen`, `GameOverDialog`, and optionally `SudokuBoard`

## Learnings (from requirements phase)
- `GameState` already has `mistakeCount: Int` and `hintsRemaining: Int` — no new fields needed for those
- `applyEnterDigit` already increments `mistakeCount`; only `isGameOver: Boolean` flag + dialog logic is missing
- `RequestHint` already decrements `hintsRemaining` but does NOT set `selectedIndex` — that wiring is required
- `redoStack` / `Redo` intent exist in state but no redo button is in scope; keep as-is
- Canvas layer order: base bg → row/col/box highlight → number-match → selected cell → conflict overlay → grid lines
- `isGameOver` flag vs sealed `GameStatus` class is an open tradeoff — resolve at design phase (flag preferred for minimal scope)
- Fixed hint slot height is unresolved — ~24dp (one text line) is a reasonable default; confirm at design phase
- No new Gradle dependencies required; all patterns supported by existing `androidx.compose.material` 1.7.3
- Circular button pattern: `clip(CircleShape)` MUST come before `.background()` or fill bleeds outside circle

## Completed Tasks
- [x] 1.1 [RED] Failing test: isGameOver set on 3rd mistake - e509380
- [x] 1.2 [GREEN] Add isGameOver to GameState; wire applyEnterDigit - b6971da
- [x] 1.3 [RED] Failing tests: TimerTick guard, PuzzleGenerated reset, RequestHint selectedIndex - 0b9904d
- [x] 1.4 [GREEN] Wire TimerTick guard, PuzzleGenerated reset, RequestHint selectedIndex, timer cancel - d32fdb5
- [x] 1.5 [GREEN] Create AppColors.kt — brand color constants - 5e327df
- [x] 1.6 [GREEN] Create GameOverDialog.kt — non-dismissible game-over dialog - c4bb9f1
- [x] 1.7 [GREEN] Modify GameScreen.kt — remove pencil, fix weights, hint slot, game-over guards - 7476e5b

- [x] 1.8 [GREEN] Modify NumberPad.kt — use AppColors for button colors - 0ca3f1e

- [x] 2.1 [GREEN] Add edge case tests: Undo/StartNewGame after game-over - 315cb85

## Current Task
Awaiting next task

## Learnings (from task planning phase)
- `HintResult.Found.targetCells: List<Int>` (confirmed from engine source) — use `targetCells.firstOrNull()` in RequestHint reducer, NOT `hint.index` or `hint.cellIndex`
- `GameViewModelTest.kt` does not yet exist in engine test dir — task 1.1 must create it; verify path before writing
- `GameState` already has `mistakeCount` and `hintsRemaining` fields (confirmed) — only `isGameOver` is missing
- `applyEnterDigit` already increments `mistakeCount` but returns `checkCompletion(newState)` unconditionally — task 1.2 must change return to skip checkCompletion when isGameOver
- `GameScreen` already partially redesigned (horizontal Row, stats row, 3×3 numpad) — task 1.7 is incremental, not a full rewrite
- Pencil `BadgedActionButton` (✏ / "OFF") still present in current GameScreen (confirmed) — removal is the main structural change in task 1.7
- NumberPad already correctly uses nested Column+Row with weight+aspectRatio — no structural change needed (task 1.8 is color-only)
- SudokuBoard excluded from scope — canvas layers already correct, AppColors adoption is optional and skipped
- TDD Red step for UI composable tasks (1.5, 1.6) omitted — no UI test framework; build verification is the only automated signal
- Total task count: 15 tasks (within coarse 10–20 target)

### Verification: V1 [VERIFY] Quality checkpoint: engine tests + build green
- Status: PASS
- Commands: ./gradlew :engine:test (exit 0), ./gradlew :app:build (exit 0)
- engine:test: BUILD SUCCESSFUL — 5 tasks, jacocoTestReport included
- app:build: BUILD SUCCESSFUL — 10 tasks, all compiled and assembled
- Zero test failures

### Verification: V2 [VERIFY] Quality checkpoint: build after new files
- Status: PASS
- Commands: ./gradlew :app:build (exit 0)
- app:build: BUILD SUCCESSFUL — 10 tasks, 10 up-to-date
- AppColors.kt: 14 val constants confirmed
- GameOverDialog.kt: file exists and compiled cleanly
- No warnings treated as errors

### Verification: 1.9 [VERIFY] FR-9 structural check: NumberPad is 3×3 grid, digits 1–9 only, no erase button inside
- Status: PASS
- Step 1: `for (row in 0..2)` found at line 33 — 3×3 loop pattern confirmed
- Step 2: `row * 3 + col + 1` found at line 39 — digit range 1–9 confirmed
- Step 3: No erase/delete/clear/backspace found — no erase button inside NumberPad
- Step 4: `weight(1f)` found at line 44 — uniform grid layout confirmed
- No code changes required; structure was already correct

### Verification: V3 [VERIFY] Full quality gate: engine tests + build
- Status: PASS
- Commands: ./gradlew :engine:test (exit 0), ./gradlew :app:build (exit 0)
- engine:test: BUILD SUCCESSFUL — 5 tasks, all up-to-date
- app:build: BUILD SUCCESSFUL — 10 tasks, all up-to-date
- Zero test failures; no regressions detected

### Verification: V4 [VERIFY] Full local CI: engine tests + app build
- Status: PASS
- Commands: ./gradlew :engine:test (exit 0), ./gradlew :app:build (exit 0)
- engine:test: BUILD SUCCESSFUL — 5 tasks, all up-to-date
- app:build: BUILD SUCCESSFUL — 10 tasks, all up-to-date
- Both commands exit 0; no test failures; no build errors

### Verification: 1.10 [VERIFY] FR-11 canvas layer check: row/col/box highlight is Layer 2 in SudokuBoard
- Status: PASS
- Step 1: "// Layer 2: row/col/box highlight" comment found at line 64 — layer tag present
- Step 2: Layer ordering confirmed: Layer 1 (line 60), Layer 2 (line 64), Layer 3 (line 76), Layer 4 (line 82), Layer 5 (line 87) — correct sequential order
- Step 3: `selRow`, `selCol`, `selBox` variables at lines 67–68; `Color(0xFFD4E8FA)` drawRect at line 72 — highlight color and logic confirmed
- Row/col/box highlight (Layer 2) correctly precedes selected-cell overlay (Layer 4) and conflict overlay (Layer 5)
- No code changes required; canvas layer order was already correct per FR-11
