---
spec: sudoku-app
basePath: ./specs/sudoku-app
phase: research
task: 0/0
updated: 2026-05-25T00:00:00Z
---

# Progress: sudoku-app

## Original Goal

The whole new Sudoku application

## Completed Tasks

- [x] 1.1 Create root Gradle settings and version catalog - 32fe3a3

## Current Task

Awaiting next task

## Learnings

- Project is a blank slate — no source files, only IntelliJ IDEA config; full stack freedom
- IntelliJ workspace.xml shows both TypeScript and Kotlin indices — developer likely comfortable with both
- Target: desktop only (Windows/Linux/macOS), single binary
- CRITICAL TENSION: Compose Multiplatform Desktop (best Kotlin GUI) requires JVM — produces a ~100-150MB installer, NOT a true single zero-dependency binary
- True single binary options for Kotlin: GraalVM native-image (Compose not supported) or TUI only
- True single binary with full GUI requires Go (Ebiten) or Rust (egui/iced) — different language from Kotlin
- ComposeArcade is the closest prior art: KMP Sudoku, MVI architecture, Android + Desktop, 99% shared code
- LibreSudoku is mature Android-only reference: 4 difficulties, stats, tutorials, custom puzzles
- Puzzle generation: Las Vegas fill + dig-holes + early-exit uniqueness check is the standard approach
- Difficulty grading: technique-based (naked singles → pairs → triples → X-Wing); no mathematical standard
- Hint system: constraint propagation solver run step-by-step, expose simplest applicable technique
- Dancing Links is fastest for uniqueness checks during generation; naive DFS is acceptable alternative
- No off-the-shelf KMP Sudoku engine library exists; must write from scratch

## Interview Responses

### Requirements Interview
- Distribution: Platform-native installer (exe/dmg/deb) — Kotlin + Compose Multiplatform Desktop + jpackage
- Language: Any language acceptable; Kotlin + Compose is the natural choice
- Persistence: Session-only — no save state between launches
- Hints: Highlight relevant cells + name/explain the technique (e.g. "Naked single at R3C5")
- Validation: Real-time conflict highlighting as user fills cells
- Platforms: Windows + macOS + Linux
- Chosen approach: Phased — MVP gameplay now; pencil marks, stats, themes, custom import deferred to v2

## Blockers

- None currently

### Verification: V1 [VERIFY] Quality checkpoint: Gradle sync
- Status: PASS
- Commands: `./gradlew tasks --all 2>&1 | grep -q 'engine\|app' && echo V1_PASS` (exit 0, output: V1_PASS)
- Details: Gradle 8.11.1 resolved all modules; both `:engine` and `:app` subproject tasks visible in task list
- Note: Java 17 (OpenJDK) used; jvmToolchain(21) in build files will use Gradle toolchain auto-provisioning when JDK 21 is available on target CI

## Next

Generate requirements.md

### Tasks Interview (from tasks phase)
- Granularity: Fine (40-60+ atomic tasks, one file/function per task, [VERIFY] every 2-3 tasks)
- E2E verification: NO -- desktop GUI app, no HTTP/CLI automation; manual verification sufficient
- Execution priority: Quality-first -- engine fully correct and tested before UI wired; POC proves game loop on single platform first
- Chosen approach: Quality-first POC -- engine foundation with correctness tests before UI, then full game loop, then polish and packaging

### Design Interview (from design phase)
- Module structure: Two Gradle modules — `engine` (pure Kotlin, no Compose dep) + `app` (Compose Desktop, state, UI)
- Uniqueness solver: Optimized backtracking with constraint propagation + early-exit on 2nd solution (no DLX)
- MVI strictness: Strict MVI — single GameViewModel, sealed GameIntent, single immutable GameState, composables are pure functions of state
- Libraries: Engine is dependency-free pure Kotlin; app uses Coroutines (bundled with Compose) for async generation; no external engine libraries exist
- Chosen approach: Two-module Kotlin + Compose Multiplatform Desktop — clean engine/UI boundary, strict MVI, optimized backtracking for generation

## Learnings
- Player clarified: given cells ARE selectable — selection triggers number highlight showing all matching digits across the board; only digit entry is blocked on givens
- Three simultaneous visual states required on cells (selection, number-match, conflict) — must be visually distinct; added as explicit FR-018 and AC-3.7
- Hint engine scope bounded at Pointing Pair for v1 — X-Wing and above return "No hint available"; this is a deliberate scope cut, not an oversight
- Difficulty grading thresholds are subjective and will need empirical tuning during implementation — flagged as unresolved question requiring stakeholder agreement on acceptance bar
- Session-only persistence simplifies architecture significantly — no GameRepository needed for save/load (architecture diagram in research.md includes it but it can be omitted)
- jpackage requires WiX on Windows build agents for .msi output — CI environment must be confirmed before pipeline setup

## Learnings (design phase — architect)
- Canvas-based board rendering chosen over LazyVerticalGrid: three simultaneous visual state layers (fill + border variation) and custom thick/thin grid lines require fine-grained draw control that Canvas provides cleanly
- IntArray(81) flat representation with bitmask candidates (int per cell, bits 1–9) avoids boxing overhead in hot conflict/hint paths; immutability is contractual not structural
- MRV (minimum remaining values) heuristic in backtracking solver eliminates need for Dancing Links — uniqueness checks empirically fast enough (<200ms); if Expert generation exceeds 2s, add forward-checking before considering DLX
- Difficulty-targeted generation uses retry loop (fill → grade → retry if mismatch) rather than targeted hole-digging — simpler implementation, converges quickly in practice (Easy/Medium: ~1 retry; Hard/Expert: ~5–20 retries)
- `HintResult.NoHintForDifficulty` is a first-class sealed variant — not null, not an error code — maps directly to the US-9 AC-9.1 message wording in the UI
- No `AndroidX ViewModel` on Compose Desktop — ViewModel lifetime is tied to `rememberCoroutineScope()` or a `LaunchedEffect`-anchored scope at the root composable
- Three cell visual states use: blue fill + solid border (selected), amber fill + crosshatch overlay (number-match), red fill + dashed border (conflict) — satisfies NFR-012 without color-only reliance
- WiX 4 confirmed needed on Windows CI runner for .msi — `choco install wixtoolset` or dedicated GitHub Action required
- `jpackage` module list (`--add-modules`) can reduce installer from ~250MB to ~100–150MB by stripping unused JDK modules

## Learnings (requirements revision — reviewer feedback)
- FR-006 Hard difficulty capped at Pointing Pairs (not Naked/Hidden Triples) so hard puzzles stay within hint engine coverage — eliminates the hint gap for Hard
- Expert puzzles (FR-007, X-Wing+) have a documented hint gap: hint engine returns "No hint available for this difficulty level" — intentional product behavior, not silent failure; traced through US-9 and AC-9.1–9.4
- US-9 added as explicit user story for Hard/Expert hint experience; FR-007 now traceable to an AC
- AC-1.2 wording tightened: "valid 9x9 puzzle with exactly one solution" — distinguishes formal uniqueness from novelty
- NFR-002 annotated: Expert generation with uniqueness checking likely requires Dancing Links; naive backtracking likely fails the < 2s target — added as Unresolved Question
- App close behavior made explicit (AC-7.6, FR-019): confirmation dialog shown on mid-game quit — product decision, not omission
- Keyboard shortcuts for Hint (AC-4.9: H key), Pause (AC-6.7: P or Space), New Game (AC-1.6: Ctrl+N) added — NFR-009 now fully traceable
- GameRepository explicitly omitted in v1 — documented as Architecture Constraint in Dependencies section
- NFR-012 added: color-blind accessibility — three cell states must differ by more than color; or documented as v1 known limitation

- [x] 1.4 Create engine source directory skeleton - 4968348
- [x] 1.5 Implement Cell.kt with index extension properties and peersOf - b8b578c
- [x] 1.6 Implement Difficulty.kt enum - 070d7b5
- [x] 1.7 Implement Board.kt flat array model - 10d0c41

## Learnings (tasks phase — task-planner)
- 53 total tasks generated across 5 phases: Phase 1 (31 tasks including 10 [VERIFY] checkpoints), Phase 2 (6 tasks), Phase 3 (7 tasks + 4 [VERIFY] checkpoints), Phase 4 (5 tasks), Phase 5 (3 tasks)
- Phase 1 ordered quality-first: engine data types → Solver → Generator → Grader → HintEngine → state layer → UI; engine must compile before any UI file
- Gradle multi-module setup (tasks 1.1–1.3) must complete before any source files; V1 checkpoint validates sync before proceeding
- `computeConflicts` function belongs in GameViewModel (not engine module) since it operates on raw IntArray from state; tests in Phase 3 test it indirectly via GameViewModel behavior
- JaCoCo coverage plugin must be added to engine/build.gradle.kts in task 3.7 — not in Phase 1 to avoid slowing the POC build
- Expert GeneratorTest timeout may need to be relaxed to 5s if MRV-based generation consistently exceeds 2s on CI runners; annotated `@Timeout(5)` fallback documented in task 3.4
- WiX 4 on CI: using `choco install wixtoolset --version=4.0.5` in workflow; fallback to `crazy-max/ghaction-setup-wix` GitHub Action if Chocolatey package unavailable
- Board.fromDigits must make defensive copies (task 2.1) — IntArray is mutable and GameState contract depends on no external mutation after construction
- VE tasks intentionally omitted (user opted out — desktop GUI, no HTTP endpoint to automate against)
- Quality checkpoints are labeled V1–V17 for easy tracking; final verification sequence is V4/V5/V6 per phase-rules.md convention

### Verification: V2 [VERIFY] Quality checkpoint: engine data types compile
- Status: PASS
- Commands: `./gradlew :engine:compileKotlin 2>&1 | grep -c 'error:' | grep -q '^0$' && echo V2_PASS` (exit 0, output: V2_PASS)
- Details: Engine module compiled successfully (UP-TO-DATE); Cell.kt, Board.kt, and Difficulty.kt all resolve without errors; zero compile errors confirmed

## Parallel Batch (tasks 1.2, 1.3)
## Completed Tasks
- [x] 1.2 Create engine/build.gradle.kts - 2bb09da

## Current Task
Awaiting next task

## Learnings
- engine/build.gradle.kts created with kotlin("jvm") plugin, JVM toolchain 21, JUnit Jupiter, and useJUnitPlatform()
## Completed Tasks
- [x] 1.3 Create app/build.gradle.kts - 5544288

## Current Task
Awaiting next task

## Learnings
- app/build.gradle.kts created with Compose Desktop plugin, Kotlin JVM, engine dependency, coroutines, and nativeDistributions for Dmg/Msi/Deb
